[x] když se nepodaří načíst auta v listingyu, tak v prvním řádku v tabulce vypsat alert s chybou (takové to červené jako v tom fomrulář) - in progress
[ ] nepouzivej absolutni pozicovani. (pouziti "translate-middle"). nauc se flex-box.
    - porad v kodu vidim `translate-middle` & `position-absolute`
    [x] ad `home.vue` kdyz mas v divu jakozto radek, a v nem jedinou prvek, tak rozhodne npotrebujes `d-flex` - tak nevim, proc si to tam pridal..
    [x] tamtez. m-auto je dalsi kulisarna, ktera neni flexbox :-) ale chapu, snazil ses vyresit. zbytecne to kombis. ty to chces asi dat na stred i vertikalne, ale tim si to hrozne ztezujes. nezapominej, ze HTML je dokument - a ten z principu ma sice sirku danou papirem, ale vysku ma takovou, kolik mas obsahu - a protoze ten je dynamicky, tak nikdy nevis, jeslti ti vystaci jedna stranka papiru, dve, tri - stejne tak v HTML principielne nevis vysku. jak udelat opravde vertikalni zarovnani nastred ti ukazu osobne. ted se na to vykasli a kdyz tam das odsazeni od vrchu treba "mt-5" tak to bude good enough. ale zarovnej to na stred horizontalne - o co mi tady slo v prve rade je, aby si pochopil to nejdulezitejsi na bootstrapu: https://getbootstrap.com/docs/5.3/layout/grid/ to je zaklad moderniho webu.
    [x] grid je postaveny nad flexboxem a muzes jit do nizsi urovne: https://getbootstrap.com/docs/5.3/utilities/flex/ a pripadne detailni vysvetleni flexboxu: https://css-tricks.com/snippets/css/a-guide-to-flexbox/
[x] dořeš smazání auta, kdyz ho smazu, tak se neaktualizuje seznam aut
    [ ] nový bugísek - nejde smazat jedno konkrétní auto v tabulce
[ ] editaace auta / cesty: cars/edit'

napady:
- tiers
- filtrování
